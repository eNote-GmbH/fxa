<div class="card">
  <header>
    {{#showQrCode}}
      <h2 id="pair-header" class="card-header">{{#t}}Connect Firefox on your mobile device{{/t}}</h2>
    {{/showQrCode}}
    {{^showQrCode}}
      <h2 id="pair-header" class="card-header">{{#t}}Sync Firefox on your phone or tablet{{/t}}</h2>
    {{/showQrCode}}
  </header>

  <section>
    <div class="error"></div>

    {{#showQrCode}}

    {{! Add toggle button between "Have Firefox" and "Download Firefox"  }}

    {{! Below options are shown depending on user selection - this might need to be a mixin? }}

    <div class="mt-5 flex justify-between p-1 border-solid border-1 border-black bg-grey-50 rounded-lg font-bold">
      <button type="button" aria-pressed="true" class="w-1/2 bg-white text-blue-500 rounded p-2">{{#t}}Have Firefox{{/t}}</button>
      <button type="button" aria-pressed="false" class="w-1/2 rounded p-2">{{#t}}Download Firefox{{/t}}</button>
    </div>

    {{^haveFirefox}}
      <div class="mt-5 mb-5">{{#unsafeTranslate}}Already have Firefox on your phone or tablet? <br />Sync your device now.{{/unsafeTranslate}}</div>
      {{! Replace image with sync devices image from figma }}
      <div class="bg-image-sync-devices" role="img" aria-label=""></div>
    {{/haveFirefox}}

    {{#haveFirefox}}
      <p class="my-5 px-16">{{#unsafeTranslate}}If you don’t have Firefox on your phone or tablet, scan the QR code to download it now. Then, select <strong>Sync device</strong>.{{/unsafeTranslate}}</p>
      <div class="bg-image-cad-qr-code" role="img" aria-label="{{#t}}QR code{{/t}}"></div>
    {{/haveFirefox}}
    {{! Validate if "Sync device" button should be shown for both "Have Firefox" and "Download Firefox" }}
    <form novalidate class="mt-5">
      <div class="flex">
        <button id="start-pairing" class="cta-primary cta-xl" type="submit">{{#t}}Sync device{{/t}}</button>
      </div>
    </form>
    <p class="mt-5 text-sm"><a id="pair-not-now" class="link-blue" href="/settings">{{#t}}Not now{{/t}}</a></p>

    {{/showQrCode}}
    {{^showQrCode}}
    <div class="{{graphicId}} mt-5" role="img" aria-label="{{#t}}Complete your set-up{{/t}}"></div>
    <p class="mt-5 mb-5 px-16">{{#t}}Take your tabs, bookmarks, and passwords anywhere you use Firefox.{{/t}}</p>
    <form novalidate>
      <div class="flex">
        <button id="start-pairing" class="cta-primary cta-xl" type="submit">{{#t}}Get started{{/t}}</button>
      </div>
    </form>
    {{/showQrCode}}
  </section>
</div>
